extends layout

block content
    h1 #{title}
    ul.list-group.list-group-flush
        each article, i in articles
            li.list-group-item 
                a(href="/articles/" + article._id)= article.title 